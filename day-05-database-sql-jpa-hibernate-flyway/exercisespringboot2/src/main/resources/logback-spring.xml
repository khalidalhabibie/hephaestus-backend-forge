<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console: JSON rapi di VS Code terminal -->
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>{"timestamp":"%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}","level":"%level","logger":"%logger{36}","correlation_id":"%X{correlation_id:-}","message":"%msg"}%n</pattern>
        </encoder>
    </appender>

    <!-- File: semua log termasuk Hibernate SQL (untuk debug) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/application.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{correlation_id}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Hibernate SQL & parameter binding: hanya ke FILE, tidak ke console -->
    <logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
        <appender-ref ref="FILE" />
    </logger>
    
    <logger name="org.hibernate.orm.jdbc.bind" level="TRACE" additivity="false">
        <appender-ref ref="FILE" />
    </logger>
    
    <logger name="org.hibernate.stat" level="DEBUG" additivity="false">
        <appender-ref ref="FILE" />
    </logger>

    <!-- Root: log aplikasi masuk ke JSON_CONSOLE dan FILE -->
    <root level="INFO">
        <appender-ref ref="JSON_CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>